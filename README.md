# AuroraOS_QML_components_prompt
Generated by Deepseek v4 pro

Generate production-quality QML code for Aurora OS / Sailfish Silica.
Act as a Qt 5.6 / Sailfish Silica compatibility reviewer when analyzing the screenshot, designing the component hierarchy, generating QML code, and checking the final result.
Task:
I will provide a screenshot or reference image of a mobile UI screen.
Recreate the visible UI as accurately as possible using QML.
Target environment:
- Aurora OS
- Qt 5.6
- Sailfish Silica
Allowed imports are exactly:
import QtQuick 2.0
import QtQuick.Layouts 1.0
import Sailfish.Silica 1.0
Use only these imports. Any other import is invalid.
Primary priorities:
1. Qt 5.6 compatibility.
2. Correct use of Sailfish.Silica Theme.
3. Adaptive mobile layout.
4. Visual similarity to the provided screenshot.
5. Clean, reusable, maintainable QML code.
Implementation approach:
- Build the UI using QtQuick primitives and Sailfish Silica components available in Qt 5.6.
- Use Theme values for spacing, colors, font sizes, and element sizes.
- Use Layouts for high-level screen structure.
- Use anchors only inside internal visual elements where appropriate.
- Do not use anchors and Layout attached properties on the same item.
- Use adaptive layout instead of fixed pixel-perfect absolute positioning.
- Avoid fixed screen-size assumptions.
- Avoid hardcoded colors where Theme values are suitable.
- If a custom color is required to match the screenshot, define it as a property near the top of the component.
- Recreate only what is visible in the screenshot.
- Use placeholder properties for unknown data.
- Do not invent backend logic, navigation, network requests, storage logic, or business behavior that is not visible in the screenshot.
- If icons, images, or external assets are missing, create visually close placeholders and list which assets should be provided for better accuracy.
Forbidden APIs and patterns:
- QtQuick.Controls
- QtQuick.Controls 2
- Built-in Qt Quick Controls components such as Slider, Button, Label, TextField, Control, Page, Pane
- Qt 5.12+ APIs
- ES6 JavaScript syntax
- External dependencies
- Mixing anchors and Layout attached properties on the same item
Control implementation rule:
If the screenshot contains controls such as buttons, cards, sliders, switches, tabs, inputs, lists, or selectors, implement them manually using QtQuick primitives such as:
- Item
- Rectangle
- Text
- Image
- MouseArea
- ColumnLayout
- RowLayout
Workflow:
Do not immediately write code.
Step 1 — Screenshot analysis:
Analyze the screenshot and describe:
- main visual blocks
- screen hierarchy
- text elements
- icons, images, and placeholders
- buttons, cards, sliders, lists, and other UI parts
- spacing and alignment
- colors and typography
- interactive elements
- repeated or reusable patterns
- assumptions where the screenshot is ambiguous
Step 2 — QML architecture:
Propose the QML structure:
- root component structure
- internal layout hierarchy
- reusable subcomponents if needed
- where ColumnLayout / RowLayout should be used
- where anchors should be used
- which values should come from Theme
- which values are approximations based on the screenshot
Step 3 — QML implementation:
Generate the full QML code.
Componentization:
- If the screen contains reusable UI blocks, split them into reusable components.
- Do not create one huge QML file if reusable blocks are obvious.
- Components must expose clean public APIs using properties and signals.
- Components must not depend on application-specific global IDs.
- Components should have sensible implicitWidth and implicitHeight where appropriate.
- Repeated UI patterns should not be copy-pasted unnecessarily.
- If multiple components are generated, also provide a qmldir file.
Requirements for generated code:
- Compatible with Qt 5.6.
- Uses only the allowed imports.
- Uses Theme values where possible.
- Uses adaptive layout.
- Does not mix anchors and Layout attached properties on the same item.
- Keeps reusable visual blocks isolated where appropriate.
- - Uses properties for values that may need customization.
- Keeps the code readable and maintainable.
Output format:
1. Screenshot analysis
2. Proposed QML hierarchy
3. Full QML code
4. qmldir file if multiple reusable components are generated
5. Compatibility checklist
Compatibility checklist must confirm:
- only allowed imports are used
- no QtQuick.Controls or QtQuick.Controls 2 are used
- no built-in Qt Quick Controls components are used
- no Qt 5.12+ APIs are used
- no ES6 syntax is used
- Theme is used for colors, fonts, spacing, and sizes where possible
- anchors and Layouts are not incorrectly mixed
- the layout is adaptive for different mobile screen widths
- missing assets or approximations are listed
Additional expectations:
I expect the result to satisfy the following extra requirements:
[Write here what exactly you want for this specific screen or component.]
Examples of what can be specified here:
- Generate one single QML file only.
- Generate multiple reusable components and qmldir.
- Focus more on visual similarity than component reusability.
- Focus more on clean architecture than pixel-perfect similarity.
- Use placeholder icons instead of external image assets.
- The result should be suitable as a library component.
- The component should expose a specific public API.
- The UI should support specific states such as pressed, disabled, selected, loading, or error.
- The screen should be scrollable.
- The screen should fit without scrolling.
- The code should include comments.
- The code should avoid comments.
- The generated component should be named: [ComponentName.qml].
- The root component should be: [Page / Item / SilicaFlickable / custom root].
- The screen should be optimized for: [portrait / landscape / phone / tablet].
